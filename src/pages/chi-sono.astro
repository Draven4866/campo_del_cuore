---
/**
 * CHI-SONO — Il Campo del Cuore
 *
 * Fix chiave:
 * - @font-face iniettati con URL assolute basate su BASE_URL (no /_astro/fonts 404)
 */

const year = new Date().getFullYear();

const baseHref = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : import.meta.env.BASE_URL + "/";

const u = (p: string): string => baseHref + p.replace(/^\/+/, "");

// link interni
const hrefHome = baseHref;
const hrefChi = u("chi-sono/");
const hrefVideo = u("video/");
const hrefCornice = u("cornice/");

// asset
const profilImg = u("img/profilo.jpg");
const bkg = u("img/chi-sono_bkg.jpg");

// fonts (public/fonts)
const spinFontUrl = u("fonts/Spin%20Cycle%20Regular.ttf");
const celtFontUrl = u("fonts/CELTG___.TTF");
const candFontUrl = u("fonts/candara.ttf");

// @font-face con URL assolute
const fontFaceCSS = `
@font-face{
  font-family:"SpinCycle";
  src:url("${spinFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
@font-face{
  font-family:"CelticGaramond";
  src:url("${celtFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
@font-face{
  font-family:"Candara";
  src:url("${candFontUrl}") format("truetype");
  font-weight:400;
  font-style:normal;
  font-display:swap;
}
`;
---

<!doctype html>
<html lang="it">
  <head>
    <meta charset="utf-8" />
    <base href={baseHref} />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Chi sono — Il Campo del Cuore</title>

    <!-- Preload font -->
    <link rel="preload" href={spinFontUrl} as="font" type="font/ttf" crossorigin />
    <link rel="preload" href={celtFontUrl} as="font" type="font/ttf" crossorigin />
    <link rel="preload" href={candFontUrl} as="font" type="font/ttf" crossorigin />

    <!-- @font-face con URL assolute -->
    <style is:global set:html={fontFaceCSS}></style>
  </head>

  <body style={`--bkg: ${bkg};`}>
    <header class="topbar">
      <a class="brand" href={hrefHome}>Il Campo del Cuore</a>
      <nav class="nav">
        <a href={hrefChi} aria-current="page">Chi sono</a>
        <a href={hrefVideo}>Galleria Video</a>
      </nav>
    </header>

    <main>
      <section class="pageHero" aria-label="Testata pagina">
        <div class="pageHeroInner">
          <h1>Chi sono</h1>
          <p>
            Scrittura, metodo e ricerca: come traduco esperienza e immaginazione in strumenti
            semplici, ripetibili e sobri.
          </p>
        </div>
      </section>

      <section class="paper" aria-label="Contenuto">
        <article class="paperInner">
          <section class="floatWrap">
            <img class="profileImg" src={profilImg} alt="" width="200" height="250" />

            <h2 class="h2First">Identità e postura</h2>
            <p>
              Mi chiamo Raffaele e sono uno scrittore con formazione artistica, oltre che uno studioso
              appassionato di esoterismo. Non sono un dottore, né uno scienziato, né un “guru”.
              Il mio lavoro parte da una scelta semplice: trattare ciò che è interiore con rispetto,
              lucidità e concretezza, senza spettacolarizzarlo e senza trasformarlo in un’etichetta identitaria.
            </p>

            <h2>Scrittura come tecnologia di traduzione</h2>
            <p>
              La scrittura mi accompagna da sempre: è stata prima un appiglio immaginifico e poi una disciplina.
              Nel tempo ho imparato a usarla come strumento di traduzione: dare forma a ciò che preme dentro,
              ordinare il caos e costruire linguaggio laddove prima c’era solo intensità.
            </p>
          </section>

          <h2>Formazione artistica e primo corpus</h2>
          <p>
            Mi sono laureato presso l’Università Ca’ Foscari di Venezia in Tecniche Artistiche dello Spettacolo (T.A.R.S.).
            Dopo la laurea, nel 2010, ho rivisto alcuni scritti composti tra il 1998 e il 2005: una raccolta di poesie
            e una serie di racconti brevi. Quel lavoro di revisione è stato un atto fondativo.
          </p>

          <h2>Musica e dimensione internazionale</h2>
          <p>
            Dal 2010 mi sono dedicato anche alla musica con un progetto hard rock/metal. Quel periodo mi ha portato
            in contesti di produzione professionale e in studi di registrazione, fino a una fase internazionale
            culminata con un debut album pubblicato ufficialmente nell’agosto del 2012.
          </p>

          <h2>Ritorno alla scrittura e formazione narrativa</h2>
          <p>
            Dal 2015 ho scelto di dedicarmi pienamente alla scrittura. Nel 2016 un incontro formativo con Franco Forte
            mi ha aiutato a mettere a terra metodo e costanza. A metà 2018 sono stato selezionato per il Master in
            Tecniche della Narrazione della Scuola Palomar (2018/2019).
          </p>

          <h2>Dalla sensibilità alla competenza di campo</h2>
          <p>
            Negli ultimi anni la mia ricerca si è concentrata su un passaggio specifico: trasformare la sensibilità
            in competenza. Ho trovato un contesto di lavoro serio e pratico in
            <a href="https://www.fucinalchemica.it" target="_blank" rel="noopener noreferrer">FucinAlchemica</a>.
          </p>

          <h2>Integrazione corpo–psiche</h2>
          <p>
            Ho integrato la dimensione somatica e il lavoro sulle radici: pratiche di radicamento presso
            <a href="https://raidho-healinghorses.com" target="_blank" rel="noopener noreferrer">Raidho Healing Horses</a>
            e un percorso sul bambino interiore con Cinzia Maino di
            <a href="https://orionmultidimensional.com" target="_blank" rel="noopener noreferrer">Orion Multi Dimensional</a>.
          </p>

          <h2>Campo Emozionale</h2>
          <p>
            Da questo intreccio nasce il concetto di Campo Emozionale: un modo per rendere leggibile ciò che il Corpo
            Emozionale “emana” nella vita quotidiana, come mappa utile e struttura pratica.
          </p>

          <h2>Etica e confini</h2>
          <p>
            Il mio lavoro non è terapia e non sostituisce un supporto professionale quando serve. È un metodo sobrio:
            funziona se resta semplice, concreto e verificabile nel quotidiano.
          </p>

          <h2>Prossimo passo</h2>
          <p>
            Se vuoi capire il tono del percorso, leggi la cornice. Se preferisci una guida in video, trovi la galleria
            nella pagina dedicata.
          </p>

          <div class="ctaRow" role="navigation" aria-label="Azioni">
            <a class="btn btnGhost" href={hrefHome} role="button">Torna alla Home</a>
            <a class="btn btnGhost" href={hrefCornice} role="button">Leggi la cornice</a>
            <a class="btn btnPrimary" href={hrefVideo} role="button">Vai alla galleria video</a>
          </div>
        </article>
      </section>
    </main>

    <footer class="footer">
      <small>© {year} Il Campo del Cuore</small>
    </footer>

    <style>
      :root{
        --chakraGreen: rgba(150, 255, 190, 0.18);
        --ghostWhite: rgba(255,255,255,0.16);
      }

      *{ box-sizing:border-box; }
      a{ color:inherit; text-decoration:none; }

      body{
        margin:0;
        color:#fff;
        font-family:"Candara", system-ui, -apple-system, Segoe UI, Roboto, Arial;
        line-height:1.5;

        background-color:#000;
        background-image: url(var(--bkg));
        background-size: cover;
        background-position: center;
        background-repeat: no-repeat;
        background-attachment: fixed;
        position: relative;
      }

      body::before{
        content:"";
        position: fixed;
        inset: 0;
        pointer-events: none;
        z-index: 0;
        background:
          radial-gradient(80% 60% at 50% 0%, rgba(0,0,0,.18), rgba(0,0,0,.62)),
          linear-gradient(to bottom, rgba(0,0,0,.10), rgba(0,0,0,.70));
      }

      header, main, footer{ position: relative; z-index: 1; }

      .topbar{
        position:sticky; top:0; z-index:60;
        display:flex; justify-content:space-between; align-items:center;
        padding:16px 22px;
        backdrop-filter:saturate(180%) blur(10px);
        background: rgba(210,235,255,.88);
        border-bottom:1px solid rgba(0,0,0,.06);
      }
      .brand{
        font-family:"SpinCycle","Candara",system-ui,sans-serif;
        font-weight:400;
        color:#0b0b0b;
      }
      .nav a{
        margin-left:14px;
        opacity:.85;
        font-family:"CelticGaramond","Candara",system-ui,serif;
        color:#0b0b0b;
      }
      .nav a:hover{ opacity:1; }
      .nav a[aria-current="page"]{
        opacity:1;
        text-decoration: underline;
        text-underline-offset: 6px;
      }

      .pageHero{
        position:relative;
        padding: 64px 22px 28px;
        overflow:hidden;
      }
      .pageHero::before{
        content:"";
        position:absolute;
        inset:0;
        background:
          radial-gradient(70% 60% at 50% 10%, rgba(0,0,0,.08), rgba(0,0,0,.55)),
          linear-gradient(to bottom, rgba(0,0,0,.05), rgba(0,0,0,.70));
        pointer-events:none;
      }
      .pageHeroInner{
        position:relative;
        max-width: 900px;
        margin: 0 auto;
        text-align:center;
      }
      .pageHero h1{
        font-family:"SpinCycle","Candara",system-ui,sans-serif;
        font-weight:400;
        margin:0 0 10px;
        font-size: clamp(34px, 5vw, 58px);
        text-shadow: 0 2px 18px rgba(0,0,0,.55);
      }
      .pageHero p{
        font-family:"CelticGaramond","Candara",system-ui,serif;
        margin:0 auto;
        max-width: 70ch;
        opacity:.95;
        font-size: 18px;
        text-shadow: 0 2px 18px rgba(0,0,0,.55);
      }

      .paper{ padding: 26px 22px 72px; }
      .paperInner{
        max-width: 900px;
        margin: 0 auto;
        padding: 22px 20px 26px;
        border-radius: 18px;
        background: rgba(20,20,20,.28);
        backdrop-filter: blur(14px) saturate(140%);
        border: 1px solid rgba(255,255,255,.22);
        box-shadow: 0 18px 55px rgba(0,0,0,.25);
      }

      .floatWrap{ overflow:auto; }
      .profileImg{
        float:left;
        margin:0 16px 12px 0;
        border:3px solid #000;
        display:block;
      }
      .h2First{ margin-top:0; }

      .paperInner h2{
        font-family:"SpinCycle","Candara",system-ui,sans-serif;
        font-weight:400;
        margin: 18px 0 8px;
        font-size: 26px;
      }
      .paperInner p{
        margin: 0 0 14px;
        max-width: 75ch;
        font-size: 18px;
        color: rgba(255,255,255,.96);
        text-shadow: 0 2px 14px rgba(0,0,0,.45);
        text-align: justify;
        text-justify: inter-word;
        hyphens: auto;
      }
      .paperInner p a{
        text-decoration: underline;
        text-underline-offset: 4px;
        text-decoration-thickness: 1px;
      }

      .ctaRow{
        margin-top: 18px;
        display:flex;
        gap:14px;
        flex-wrap:wrap;
        justify-content:center;
      }
      .btn{
        display:inline-flex;
        align-items:center;
        justify-content:center;
        padding: 12px 18px;
        border-radius: 999px;
        font-family: "CelticGaramond","Candara",system-ui,serif;
        font-weight: 600;
        line-height: 1;
        border: 1px solid rgba(255,255,255,.70);
        backdrop-filter: blur(8px);
        cursor:pointer;
        user-select:none;
        transition: transform .12s ease, filter .12s ease, background-color .12s ease;
      }
      .btnPrimary{
        background: var(--chakraGreen);
        color:#0b0b0b;
        border-color: rgba(0,0,0,.70);
      }
      .btnGhost{
        background: var(--ghostWhite);
        color:#fff;
      }

      .footer{
        padding:30px 22px;
        border-top:1px solid rgba(255,255,255,.18);
        opacity:.85;
        text-shadow: 0 2px 18px rgba(0,0,0,.55);
      }

      @media (max-width: 900px){
        .paperInner{ padding: 20px 16px 24px; }
        .paperInner h2{ font-size:24px; }
        .paperInner p{ font-size:17px; }
      }
    </style>
  </body>
</html>
